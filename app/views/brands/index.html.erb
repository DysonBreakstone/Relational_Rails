<% @brands.each do |brand|%>
  <p><%= brand.name %><%= link_to "Edit Brand", "/brands/#{brand.id}/edit"%></p>
  <p><%= brand.founded %></p>
  <p><%= brand.backpacks_only %></p>
<% end %>
<%= link_to "New Brand", "/brands/new"%>
